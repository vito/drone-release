set -e -x

mkdir -p ${BOSH_INSTALL_TARGET}/bin/

chmod +x docker/docker-0.10.0

cp docker/docker-0.10.0 ${BOSH_INSTALL_TARGET}/bin/docker

cp docker/aufs-tools_20110410-1_amd64.deb ${BOSH_INSTALL_TARGET}
