set -e -x

mkdir -p ${BOSH_INSTALL_TARGET}/bin/

chmod +x docker_0.8.0/docker-0.8.0

cp docker_0.8.0/docker-0.8.0 ${BOSH_INSTALL_TARGET}/bin/docker

cp docker_0.8.0/aufs-tools_20110410-1_amd64.deb ${BOSH_INSTALL_TARGET}
